//! Переменные

// Переменные с инициализацией
var myName = "Tom";

var age = 27;

var isOld = false;

var job = null;

// Переменная без инициализации
// (будет undefined)
var ufo;

// Константы
const PI = 3.14;

// Новый способ объявления переменных.
// Поговорим о нем немного позже
let lastName = "Smith";